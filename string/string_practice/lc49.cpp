#include <iostream>
#include <vector>
using namespace std;

vector<vector<string>> groupAnagrams(vector<string>& strs) {
        vector<string> ans;
        vector<vector> count ;
        for(int i = 0; i<)
        
    }

int main(){
 
}
